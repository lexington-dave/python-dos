# python-dos
## How to Use
1. install rich
   `pip install rich`
1. Clone this repository:
   `git clone https://github.com`
2. Navigate to the folder:
   `cd YOUR_REPO_NAME`
3. Run the script:
   `python pdos.py`
